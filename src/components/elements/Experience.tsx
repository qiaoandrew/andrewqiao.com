type ExperienceProps = {
  company: string;
  role: string;
  date: string;
  location: string;
  points: React.ReactNode[];
};

export default function Experience({
  company,
  role,
  date,
  location,
  points,
}: ExperienceProps) {
  return <div></div>;
}
